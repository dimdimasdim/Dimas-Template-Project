package com.dimas.dimasproject.core.presentation

/**
 * Marker interface for all user intents / events (Intent in MVI).
 * Every screen's intent sealed interface must implement this.
 */
interface BaseIntent

